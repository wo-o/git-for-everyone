{
  "description": "Git 가드레일 — 원격 작업 차단 + main/master 브랜치 워크플로우 안내",
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "command": "/usr/bin/env bash ${CLAUDE_PLUGIN_ROOT}/hooks/github-guardrail.sh",
            "timeout": 5000
          },
          {
            "type": "command",
            "command": "/usr/bin/env bash ${CLAUDE_PLUGIN_ROOT}/hooks/workflow-guide.sh",
            "timeout": 5000
          }
        ]
      }
    ]
  }
}
